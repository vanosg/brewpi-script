# Do not edit this file. These are default settings
# You can change the settings in config.cfg. That file will overrule the defaults set here.

scriptPath = string(default="/home/brewpi/")
wwwPath = string(default="/var/www/")
port = string(default="/dev/ttyACM0")
altport = string(default="/dev/ttyACM1")
boardType = string("leonardo", "standard", deafult="leonardo")
beerName = string(default="My First BrewPi Run")
interval = float(default=120.0)
dataLogging = string(default="active")
arduinoHome = string(default="/usr/share/arduino/")
avrdudeHome = string(default="hardware/tools/")
avrsizeHome = string(default="")
avrConf = string(default="avrdude.conf")
